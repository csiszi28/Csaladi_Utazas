/** Match TripFilterChips size: readable on desktop, touch-friendly on mobile. */
export const TRIP_SECTION_BTN_CLASS =
  "min-h-[var(--touch-target)] w-full shrink-0 rounded-lg px-3 py-2 text-sm font-medium sm:h-9 sm:min-h-9 sm:w-auto sm:py-1.5";

/** Footer actions in create/edit drawers — same scale as section / filter chips. */
export const TRIP_DIALOG_BTN_CLASS =
  "min-h-[var(--touch-target)] w-full rounded-lg px-3 py-2 text-sm font-medium sm:h-9 sm:min-h-9 sm:py-1.5";
