{
  "name": "@csaladi-utazas/database",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "prisma": "^6.11.1",
    "typescript": "^5.8.3"
  }
}
